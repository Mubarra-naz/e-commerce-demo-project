<div class="container p-5">
  <div class="card">
    <div class="card-header bg-primary text-light">
      <h2><%= @category.titleize_name %></h2>
    </div>

    <%= render 'category_form' %>

    <div class="card-footer mt-3 bg-primary">
      <%= render 'admin/shared/links', item: @category %>
    </div>
  </div>
</div>
