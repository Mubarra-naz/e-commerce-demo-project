<div class="container p-5">
  <div class="card">
    <div class="card-header bg-primary text-light">
      <h2><%= @product.titleize_title %></h2>
    </div>

    <%= render 'form' %>

    <div class="card-footer mt-3 bg-primary">
      <%= render 'shared/links', item: @product %>
    </div>
  </div>
</div>
