<p>Welcome <%= @resource.firstname %> <%= @resource.lastname %>!</p>
<p>You have signed up for the website with email <%= @email %></p>
<p>Your username for the website is "<%= @resource.username %>" </p>
<p>You can confirm your account email through the link below:</p>

<p><%= link_to 'Confirm my account', confirmation_url(@resource, confirmation_token: @token) %></p>
